# Copy this file to .env.local and fill in your Supabase project's values
# (Project Settings → API in the Supabase dashboard). Without these,
# the app runs in demo mode with mock data instead of a real database.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
